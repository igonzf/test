# Tests de Estudio

App estática para practicar tests.

Se publica en GitHub Pages y carga los bancos de preguntas JSON desde `questions/`.
Las estadísticas se guardan solo en el navegador mediante `localStorage`.
